"""
SOLO ROCK Diagnostics — Real-time detection of software→hardware communication issues.

Detects:
- Retry storms (software retrying when hardware busy)
- Queue buildup (backpressure not working)
- Thermal mismanagement (temp spikes beyond workload)
- Backpressure breakdown (software doesn't slow when hardware busy)
- Dispatch redundancy (duplicate tasks in queues)
"""
